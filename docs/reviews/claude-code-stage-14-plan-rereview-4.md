Not approved

- Critical:
  - `docs/superpowers/specs/2026-06-12-stage-14-entity-watchlist-pack-design.md:58-60` and `docs/superpowers/plans/2026-06-12-stage-14-entity-watchlist-pack-plan.md:509-518` instruct the implementation to document a workflow that includes `uv run fashion-radar collect`. The prompt explicitly says the plan must not add or document source-acquisition workflows, and review question 6 asks whether docs avoid platform/source acquisition instructions. Even if `collect` is an existing command, adding a new entity-pack guide that tells users to run collection is source-acquisition workflow documentation. Fix: remove `collect` from the entity-pack docs plan, or replace the workflow block with wording such as “after your existing configured-source/local-signal pipeline has produced signals, run match/report/candidates/trends.” Keep any collection guidance in pre-existing source docs, not the entity-pack pack docs.

- Important:
  - `docs/superpowers/plans/2026-06-12-stage-14-entity-watchlist-pack-plan.md:590-596` asks for a Claude Code review command without `--permission-mode plan`, even though this is intended to be a read-only review artifact. Fix: make the planned implementation review command explicitly read-only/planning mode, e.g. `claude -p --effort max --permission-mode plan < docs/reviews/claude-code-stage-14-code-review-prompt.md | tee docs/reviews/claude-code-stage-14-code-review.md`, and ensure the generated review prompt repeats the no-edit/no-network/no-collector/no-DB constraints.
  - `docs/superpowers/plans/2026-06-12-stage-14-entity-watchlist-pack-plan.md:556-567` uses a wording guard that includes banned terms but allows negative matches. This is useful, but insufficient by itself because it does not specifically catch the most important violation above: new documentation of the `collect` command in the entity-pack guide. Fix: add a targeted guard/review checklist item that entity-pack docs do not introduce `collect`, platform/community acquisition, scraping, or source setup instructions; if `collect` appears, require manual justification that it is only in a boundary statement, not a workflow instruction.

- Minor:
  - `docs/superpowers/plans/2026-06-12-stage-14-entity-watchlist-pack-plan.md:421-445` plans category aliases like `Mary Janes` and `boat shoes` that the existing matcher will accept without context because only single-word aliases and `UNSAFE_COMMON_ALIASES` require context. The plan partially addresses this by keeping aliases narrow, and the proposed generic rejection test covers several risky entities, but it does not test category false positives such as “Mary Jane joined…” or “wear boat shoes on deck.” Consider adding one or two negative tests for category aliases or explicitly documenting why those accepted matches are acceptable for a fashion-only configured corpus.
  - `docs/superpowers/plans/2026-06-12-stage-14-entity-watchlist-pack-plan.md:589` includes `codegraph status`, while the Stage 14 design says “CodeGraph status.” That is fine if the CLI exists in this repo, but for GitHub-upload verification it would be clearer to mark CodeGraph status as a local developer-environment check rather than a portable project verification requirement.

Overall judgment: The optional static entity watchlist pack is the right next stage after the community import contract, and keeping the default starter config unchanged is the right boundary. The proposed pack can be useful as a local seed watchlist without claiming hotness or market-wide demand, and the tests mostly cover loading, type mix, parent-brand references, default-template preservation, and matcher guardrails through the real schema. However, the planned entity-pack docs currently cross the explicit source-acquisition boundary by adding a `collect` workflow command, so the plan should be corrected before implementation.
