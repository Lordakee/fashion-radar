The `Write` tool isn't enabled in this context (read-only review), and the plan/exit tools aren't available either — so I'll deliver the review directly as requested. My analysis is complete; here is the review.

# Final Mainline Release Review (Stage 253)

> Scope: READ-ONLY review of the full social-acquisition mainline (`e8567fc..HEAD`, Stages 211–253). HEAD `556198b` is Stage 252 (YouTube collector); **Stage 253 is the uncommitted working tree** (docs + release wrap: CHANGELOG, architecture, cli-reference, source-boundaries, source-packs, the YouTube design spec, and the boundary-guard test). Review covers HEAD + that working tree. The enumerated release gate (1602 pytest, ruff, hygiene, lock/sync, packaging, installed-wheel smoke, diff checks) is taken as already green per the prompt and was not re-run.

**Verdict:** APPROVE_WITH_NITS

## Critical
None. No release-blocking defect in the social-acquisition capability.

## Important
1. **YouTube source-packs section is missing the per-collector field-shape "live-verification" caveat (non-blocking, tied to Q6).** Stage 253's own plan (`2026-06-30-stage-253-youtube-docs-wrap-plan.md`, in-scope line 15) lists a "live-verification caveat" as a deliverable for the new `## YouTube (Opt-In)` section, and Xiaohongshu / Instagram / Twitter each carry one (`source-packs.md:134-136, 159-160, 181-182`: *"field names/shape can vary by version; on your first live run, inspect the output if fields look off"*). The YouTube section (`source-packs.md:184-207`) has only the generic boundary caveat plus the (good) live-collector-vs-`yt_dlp`-adapter disambiguation note — it never tells the user to inspect `yt-dlp --dump-json` output on first live run. It does **not** block release: the collector is fail-safe (skips malformed lines, falls back on bad dates), yt-dlp's `--dump-json` schema is the most stable of the four tools, and framing is otherwise complete. Recommend one parity sentence to fully meet the plan's deliverable and Q6.

## Nits
1. **`architecture.md` "## Source Boundary" reads stale** (`architecture.md:418-425`): still names only "Phase 2: Xiaohongshu" as the opt-in social example now that Phases 3–5 are complete. Cosmetic — the Collectors bullet (`architecture.md:56`) lists all four social, and the section links to `source-boundaries.md` (complete source of truth). Updating the prose would also require touching its guard `test_architecture_boundary_docs.py`, which pins "phase 2: xiaohongshu".
2. **Doc-guard asymmetry for the four social opt-ins.** `tests/test_source_boundaries_docs.py` has dedicated opt-in assertions for Instagram, Twitter, and YouTube but none for Xiaohongshu (XHS opt-in framing is guarded instead via `architecture.md` in `test_architecture_boundary_docs.py`). Harmless; a dedicated XHS assertion would make the four guards symmetric.
3. **`youtube.py` `_upload_date` has a dead `timestamp` fallback key** (`youtube.py:104,123-131`): it tries `upload_date`/`release_date`/`timestamp` but only parses the `YYYYMMDD` form. yt-dlp's `timestamp` is a Unix-epoch integer, so that key can never parse and silently falls back to run time. Fail-safe and effectively unreachable in `ytsearch` dumps (where `upload_date` is reliably present), but the key is misleading — either parse the epoch or drop it. (No test exercises this path.)

## Summary
The full mainline (Stages 211–253) is coherent and shippable. All 5 acquisition targets are correctly tiered: official sites & news (RSS/RSSHub/HTML/sitemap) in **Core**, and the four social platforms (Xiaohongshu/Instagram/Twitter/YouTube) as **Opt-In** — note "5 targets" = 1 web/core + 4 social/opt-in, so the Opt-In list correctly holds 4 social entries. `_default_collectors` (`workflows.py:122-133`) registers all 9 collectible types; `SourceType` has 10 members (MANUAL_IMPORT is import-only, rejected by the model validator).

Point-by-point:
1. **Coherent / opt-in / fail-closed / no FR credential handling — yes.** Every social collector returns `skipped` on missing tool, auth/connection error, or non-zero exit (`youtube.py:38-48`, `twitter.py:52-74`, `instagram.py:39-56`, `xiaohongshu.py:160-174`); the runner wraps the rest as `failed` (`runner.py:88-96`). Fashion Radar handles no login/cookies/passwords: YouTube has no auth at all (no `login_user` field), Twitter never touches the browser cookie session, Instagram only reuses a saved instaloader session, Xiaohongshu defers login to the external MCP server.
2. **Boundary framing consistent — yes**, modulo Important #1 and Nit 1. "no demand proof and no platform coverage verification" + use-at-your-own-risk/opt-in appear across source-boundaries.md, source-packs.md, cli-reference.md, architecture.md, and CHANGELOG; YouTube correctly omits "login-required" (public data). **No shipped config enables a social type** — the starter pack is rss+gdelt only, so opt-in/not-by-default holds.
3. **No new Python deps — confirmed.** `pyproject.toml` deps unchanged; instaloader is an optional `try/except` import (not declared), yt-dlp/twitter-cli are external CLIs via subprocess, xiaohongshu uses the already-present `httpx`, and trafilatura remains the optional `article` extra.
4. **No schema change; v0.1.x unregressed — confirmed.** `SCHEMA_VERSION = 5` (already shipped); `source_type` is a plain `String(64)` column with no enum constraint, so new `StrEnum` members need no migration. `CollectedItem` shape is unchanged; collectors are additive and the runner correctly excludes HTML/SITEMAP + all four social from the trafilatura article-enrichment path (`runner.py:76-85, 105-112`), leaving RSS/GDELT/match/score/report intact.
5. **Release-blocking defects — none.**
6. **Live-verification caveats — acceptable for release.** Each collector is mock-tested with fail-closed, field-shape, and snippet-hygiene cases (`tests/test_collectors_{youtube,twitter,instagram,xiaohongshu}.py`), and all social summaries pass through `report_safe_snippet` (whitespace-collapsed + 500-char cap), re-applied at the report layer (`models/report.py:13-21,51-54`). The only gap is YouTube's missing field-shape caveat (Important #1) — a one-line docs follow-up, not a blocker.

**Recommendation: ship.** Optionally fold the YouTube live-verification caveat (Important #1) into the Stage 253 docs commit for parity with the other three social collectors; the three Nits are post-release polish.
