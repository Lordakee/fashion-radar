# Claude Code Stage 294 Code Review

Claude Code was attempted as the primary reviewer with `--effort max`,
read-only plan mode, and no session persistence.

Result: unavailable.

Reason: `Failed to authenticate. API Error: 401 API key is disabled`.

Fallback review: `docs/reviews/opencode-stage-294-code-review.md`.
