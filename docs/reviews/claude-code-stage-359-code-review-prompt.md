You are reviewing /home/ubuntu/fashion-radar Stage 359.

Goal:
- Add a generated-site homepage-only ROW ONE section named "Daily Local Heat Signals".
- The section should organize current-edition heated fashion brands/products from existing saved local article text instead of adding another link-only surface.

Technical stack:
- Python package with Pydantic models.
- HTML/CSS generated by src/fashion_radar/row_one/templates.py.
- Site renderer in src/fashion_radar/row_one/render.py.
- Tests in pytest.

Implementation boundary:
- Use existing app_payload["daily_digest"]["briefing_topics"].
- Do not add new app payload fields, schemas, JSON artifacts, source fetching, scoring, LLM calls, connectors, scheduling, analytics, personalization, recommendations, or compliance review.
- Homepage only; not article library, article detail pages, local article pages, or generated contract JSON.

Required behavior:
- Keep only topic_type "brand" and "product".
- Heat gate is strict positive integer: positive_heat_delta_sum > 0 or max_heat_delta > 0; bool, float, string, zero, and None must not count.
- Render only topics with at least one safe story id, usable saved local article paragraphs, and a generated local article page href.
- Link to articles/<story-id>.html#local-article-digest using safe first-class local article page filenames.
- Sort topics by positive_heat_delta_sum desc, max_heat_delta desc, local article count desc, evidence_count desc, title.
- Cap topics at 6 and story rows per topic at 2.
- Product subtype badges should surface bag/shoe/footwear/sneaker/boot/flat/heel/sandal variants from source_refs.
- Insert after Daily Local Signal Momentum and before Saved Article Content Organization.

Please review the current uncommitted diff from HEAD.

Focus on:
- Correctness against the behavior above.
- Security/escaping and unsafe href handling.
- Defensive handling of malformed raw app payload dicts.
- Tests: meaningful coverage vs brittle or false-positive assertions.
- Generated-site-only boundary and artifact guards.
- Formatting/lint risks.

Return findings by severity with file:line references. If there are no blocking issues, say so and list residual risks.
