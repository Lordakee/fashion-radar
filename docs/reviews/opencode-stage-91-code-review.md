I'll review Stage 91 systematically. Let me start by reading the relevant files and checking scope.
Now let me read the actual data-retention doc and the review artifacts to verify the assertions match the actual doc content.
Let me check the git status to verify scope, and run the verification commands in parallel.
Scope is clean - only allowed files present. Let me verify the independence claim and check the code-review.md content (the one being generated by this very review).
All verification passes. I have everything needed for the review.

# Stage 91 Code Review

Reviewer: opencode (`zhipuai-coding-plan/glm-5.2`, variant `max`), read-only.
Scope: `tests/test_data_retention_docs.py` + Stage 91 spec/plan/review artifacts, cross-checked against `docs/data-retention.md`, the Stage 90 sibling `tests/test_daily_digest_docs.py`, and the two runtime cleanup tests named in verification.

## Verdict

**No Critical blockers. No Important blockers.** The implementation matches the plan byte-for-byte, scope is clean, all verification passes, and the new test is structurally independent of runtime cleanup and CLI docs guards. Ship-ready.

## Verification Re-Run (this review)

- `pytest tests/test_data_retention_docs.py -q` → `1 passed in 0.01s`
- `pytest ... test_clean_old_data_prunes_by_collected_at test_clean_old_data_command_prunes_old_items tests/test_data_retention_docs.py -q` → `3 passed in 0.85s`
- `ruff check` + `ruff format --check` on the new file → `All checks passed!` / `1 file already formatted`
- `git diff --check` → clean
- `git status --porcelain` → only the 7 allowed files (new test + 6 Stage 91 spec/plan/review artifacts); no `docs/data-retention.md`, `src/`, schemas, `uv.lock`, CI, `tests/test_cli_docs.py`, or runtime cleanup test edits.

## Findings (ordered by severity)

### Critical
None.

### Important
None.

### Minor
None.

### Informational (non-blocking)

1. **All 14 assertions verified present in current `docs/data-retention.md`.** Spot-checked each phrase → line: `Use \`clean-old-data\`...` L14; `as_of - retention_days` L24; `Rows in \`items\`...` L27; `explicitly deletes related \`item_entities\`...` L31; `does not rely on SQLite foreign-key cascade behavior` L32; `` `--dry-run` reports... `` L34; `The cleanup command does not prune:` L39; `` `collector_runs` `` L41; `` `source_health` `` L42; `` `entity_first_seen` `` L43; `generated Markdown or JSON report files` L44; `config files` L45; `` `entity_first_seen` is intentionally retained... `` L53; `Back up the SQLite database...` L69. All match exactly.

2. **Raw substring matching diverges from Stage 90 sibling, intentionally.** `tests/test_data_retention_docs.py:16-31` uses exact `phrase in text`, whereas Stage 90's `tests/test_daily_digest_docs.py:13-15` normalizes via `" ".join(text.split()).casefold()`. The plan review already recorded this as a deliberate choice (`docs/reviews/opencode-stage-91-plan-review.md:25-32`). All 14 anchors are exact-match stable today; acceptable for a drift guard.

3. **Multiplicity is fine.** `` `collector_runs` ``, `` `source_health` ``, and `` `entity_first_seen` `` each appear multiple times in the doc (e.g., L41+L47, L42+L47, L43+L51+L53+L54). Because the assertions are existence checks (`in`), not uniqueness checks, this cannot false-positive or false-negative on drift. No change needed.

4. **`docs/reviews/opencode-stage-91-code-review.md` currently holds in-flight preamble.** It contains 3 lines of streaming text from this very review session because it is the redirection target of the currently-running review command. It will be overwritten atomically when this command completes. Not a defect — flagging only so it is not mistaken for truncation during inspection.

## Answers To Review Questions

1. **Does the implementation match the Stage 91 plan and scope?**
   Yes. `tests/test_data_retention_docs.py:1-32` is byte-identical to the body in `docs/superpowers/plans/2026-06-18-stage-91-data-retention-docs-boundary-plan.md:40-73`. Scope matches the spec's allow/deny lists exactly (`docs/superpowers/specs/...-design.md:10-23`); `git status` confirms only allowed files changed.

2. **Are the docs assertions present, stable enough, and limited to data-retention cleanup boundaries?**
   Yes. 13 of 14 phrases are backticked identifiers, a literal formula, or full sentences; `config files` (`tests/test_data_retention_docs.py:27` → `docs/data-retention.md:45`) is the only generic token and appears exactly once in its bullet. All anchors are boundary-scoped: cleanup cutoff, explicit `item_entities` pruning, dry-run reporting, non-pruned surfaces, `entity_first_seen` retention, and backup guidance. No out-of-scope topics asserted.

3. **Is the new standalone test independent from runtime cleanup tests and broad CLI docs tests?**
   Yes. The module imports only `from __future__ import annotations` and `from pathlib import Path` (`tests/test_data_retention_docs.py:1-3,9-10`); no application modules, no SQLite, no shared fixtures, no conftest coupling. The two runtime tests named in verification (`tests/test_workflows.py::test_clean_old_data_prunes_by_collected_at`, `tests/test_cli.py::test_clean_old_data_command_prunes_old_items`) are only *run* alongside it, not imported or modified. `tests/test_cli_docs.py` is orthogonal (path/flag consistency, not boundary phrases) and untouched.

4. **Are there any Critical or Important issues that must be fixed before final verification, commit, and push?**
   **No Critical. No Important.** Proceed to Task 4 (full verification, commit, push).
